# Plan 04-02: Advanced Borders, Drop Shadows & Inspector Controls

**Phase**: OSAM-04-shape-presets-clipping-masks-border-styling  
**Plan**: 04-02  
**Requirements**: SHAPE-03, SHAPE-04, SHAPE-05  
**Status**: Ready to Execute  
**Date**: 2026-09-22  

---

## Overview

Expand border styles (solid, dashed, double) and drop shadows (offset, blur, opacity, color), and upgrade `ShapesBordersSection.tsx` with interactive preset selectors (Rect, Rounded, Circle, Hexagon, Octagon, Star, Scallop, Heart) and custom SVG upload.

---

## Tasks

1. **Extend `PhotoFrameElement` in `src/domain/editor.ts`**:
   - `borderStyle?: 'solid' | 'dashed' | 'double'`
   - `borderAlignment?: 'inner' | 'center' | 'outer'`
   - `shadowEnabled?: boolean`
   - `shadowColor?: string`
   - `shadowBlur?: number`
   - `shadowOffsetX?: number`
   - `shadowOffsetY?: number`
   - `shadowOpacity?: number`

2. **Update `ShapesBordersSection.tsx`**:
   - Shape Preset Grid with all 8 presets (Rect, Rounded, Circle, Hexagon, Octagon, Star, Scallop, Heart) using Lucide icons.
   - Border Style selector (Solid, Dashed, Double) and Alignment (Inner, Center, Outer).
   - Drop Shadow card: Toggle, X/Y Offset sliders, Blur radius slider, ColorPicker, and Opacity slider.
   - Custom SVG upload / file input button allowing loading SVG paths.

3. **Render Borders & Shadows in `KonvaEditorCanvas.tsx`**:
   - Render border outline matching the shape outline (e.g. Hexagon border around Hexagon frame).
   - Apply Konva `shadowColor`, `shadowBlur`, `shadowOffset`, `shadowOpacity` to outer container.

4. **Add Unit Tests in `tests/borders.test.ts` & update `package.json`**:
   - Test stroke dash arrays, alignment calculations, and shadow property validation.

5. **Commit Plan 04-02**:
   - `git add -A && git commit -m "feat(shapes): implement advanced borders, drop shadows, and inspector shape controls (Plan 04-02)"`

---
